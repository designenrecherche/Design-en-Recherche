'use strict';

/**
 * @ngdoc function
 * @name derCleanApp.controller:ReseauCtrl
 * @description
 * # ReseauCtrl
 * Controller of the derCleanApp
 */
angular.module('derCleanApp')
  .controller('ReseauCtrl', function ($scope) {
    $scope.pageClass = 'page-reseau';
  });
