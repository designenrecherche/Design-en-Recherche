'use strict';

/**
 * @ngdoc function
 * @name derCleanApp.controller:EvenementsCtrl
 * @description
 * # EvenementsCtrl
 * Controller of the derCleanApp
 */
angular.module('derCleanApp')
  .controller('EvenementsCtrl', function ($scope) {
    $scope.pageClass = 'page-evenements';
  });
