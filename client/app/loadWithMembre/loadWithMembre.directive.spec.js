'use strict';

describe('Directive: loadWithMembre', function () {

  // load the directive's module
  beforeEach(module('designEnRechercheApp'));

  var element,
    scope;

  // beforeEach(inject(function ($rootScope) {
  //   scope = $rootScope.$new();
  // }));

  // it('should make hidden element visible', inject(function ($compile) {
  //   element = angular.element('<load-with-membre></load-with-membre>');
  //   element = $compile(element)(scope);
  //   expect(element.text()).toBe('this is the loadWithMembre directive');
  // }));
});
